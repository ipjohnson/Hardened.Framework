{
  "//": [
    "The SDK this application is built with, pinned so it does not move when a preview is installed.",
    "",
    "Without this the newest installed SDK wins, and that is not a safe default here: a net8.0",
    "project built by the .NET 11 preview SDK produces a test assembly whose deps.json is missing",
    "its own ProjectReference, so `dotnet build` succeeds and `dotnet test --no-build` fails every",
    "test at once with FileNotFoundException on the assembly under test. Nothing about the message",
    "points at the SDK.",
    "",
    "rollForward latestFeature so a newer 8.0.x patch is picked up without editing this file, and a",
    "different major is not."
  ],
  "sdk": {
    "version": "8.0.401",
    "rollForward": "latestFeature"
  }
}
